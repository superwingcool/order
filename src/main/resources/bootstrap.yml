eureka:
  client:
    service-url:
      defaultZone: http://localhost:9001/eureka

server:
  port: 9301

spring:
  application:
    name: order
  cloud:
    config:
      discovery:
        service-id: config-server
        enabled: true
      #profile: dev